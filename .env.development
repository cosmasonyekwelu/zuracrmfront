# Frontend dev server talks to your local API
VITE_API_URL=http://localhost:4000
# We are NOT using cookie sessions; skip /auth/me when logged out
VITE_USE_COOKIES=false

# (optional, for UI only)
VITE_APP_NAME=ZuraCRM
